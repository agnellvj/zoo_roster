import ServicesModule from './services'

describe('ZooRoster', () => {
  let $rootScope, $compile, _ZooRoster, ZooRoster;

  // beforeEach(inject(($injector) => {
  //   $rootScope = $injector.get('$rootScope');
  //   $compile = $injector.get('$compile');
  //   ZooRoster = $injector.get('ZooRoster');
  //   _ZooRoster = ZooRoster();
  // }));

  // beforeEach(window.module(ZooRoster));

  // describe('fetch', () => {
  //   it('Zoo Roster should be fetched and stored to the service', () => {
  //     expect(_ZooRoster.roster.length).toBe(12);
  //   });
  // });


});
